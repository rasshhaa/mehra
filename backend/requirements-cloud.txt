# Slim cloud deps — engine uses Hugging Face Inference API (set HF_TOKEN)
# Requires Python 3.11 (see runtime.txt) — inference-sdk does not support 3.14
fastapi
uvicorn[standard]
python-multipart
opencv-python-headless
reportlab
requests
inference-sdk>=0.9.9,<1.0.0
numpy
firebase-admin
